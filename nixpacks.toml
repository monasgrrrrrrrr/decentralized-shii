# Railway / Nixpacks build configuration
# PHP 8.2 + extensions needed by this project

[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo_mysql", "php82Extensions.mbstring", "php82Extensions.fileinfo", "php82Extensions.openssl"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t /app"
